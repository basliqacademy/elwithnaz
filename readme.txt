git -> version tracker

github -> single source of truth
gitlab

---
git init -> to initialize a git repository

1- untracked/unsaved changes
2- stage -> git add .
3- commit -> git commit -m "message"

git clone git@github.com:basliqacademy/elwithnaz.git -> to create a local copy of the
online repository
git push -> to push local changes to the online repository
git pull -> to get the latest changes from the online repository